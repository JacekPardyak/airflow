Trick to use public git-hub repository while working with sensitive data.
After PULL files are decrypted
Before PUSH files are encrypted
Additionally scripts (.R) and data (.csv) are cloaked in .gitignore
